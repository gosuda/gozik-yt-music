# Python bytecode caches
__pycache__/
*.py[cod]
*.pyo

# Generated protobuf stubs are now committed so the source tarball is runnable
# without the sibling gozik repository. Run `bash codegen.sh` before committing
# when the .proto definitions change.

# Virtual environment
.venv/
venv/
env/

# Distribution / packaging
*.egg-info/
dist/
build/

# Nuitka compiled output
*.dist/
*.onefile-build/

# Credential files (never commit; stored at ~/.config/gozik/)
ytmusic_auth.json
ytmusic_oauth.json
.aider*

# External tooling / bundled binaries
.tools/
certifi/

# Build artifacts / stray files
sys
Building
*.dist/
*.onefile-build/
